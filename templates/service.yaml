apiVersion: v1
kind: Service
metadata:
  name: {{ include "dino-octopus-aks.fullname" . }}
  labels:
    {{- include "dino-octopus-aks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dino-octopus-aks.selectorLabels" . | nindent 4 }}
