apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dino-octopus-aks.fullname" . }}-test-connection"
  labels:
    {{- include "dino-octopus-aks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dino-octopus-aks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
